#include <stdio.h>
/**
 * main - takes the instruction to print
 * Return: 0 if its a success
 */
int main(void)
{
printf ("with proper grammar, but the outcome is a piece of art,");
return (0);
}    
