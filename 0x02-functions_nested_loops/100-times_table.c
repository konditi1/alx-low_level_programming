/**
* print_times_table - prints the n times table, starting with 0
* @n: the highest value in the times table
*/
void print_times_table(int n)
{
if (n < 0 || n > 15)
return;

for (int i = 0; i <= n; i++)
{
printf("%4d", i * j);
}
printf("\n");
}
}

