#include "main.h"

/**
* _abs -  computes the absolute value of an integer.
* @int: any positive or negative parameter
* Return: an int if succesful
*/

int _abs(int r)
{
if (r < 0)
{
return (-r);
}
else
return (r);
}
